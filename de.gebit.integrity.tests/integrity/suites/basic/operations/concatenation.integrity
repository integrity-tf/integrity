import integrity.operations.default.*
import integrity.fixtures.basic.noop.*

packagedef integrity.basic.operations with

	suitedef concatenation with
	
		variable localVar
	 
		call echoString string: ["foo", "bar" concatenated] -> localVar
		test echoStringTest string: [1, "2" concatenated with localVar] = "12foobar"
		test echoStringTest string: "abcd" = ["a", ["b", "c" concatenated], "d" concatenated]
		
	suiteend

packageend